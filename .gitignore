__pycache__/
*.pyc
.venv/
venv/
.env
*.log
.pytest_cache/

# Operator-mounted override, never committed.
# config/defaults.env IS committed on purpose - it carries the dev-schema
# defaults that ship in the image.
config/db.env
